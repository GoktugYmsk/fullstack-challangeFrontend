import React from 'react'
import { useParams } from 'react-router-dom'

import './index.scss'

function User() {

    const { userId } = useParams()

    return (
        <div>

        </div>
    )
}

export default User
